const Footer = () => {
  return (
    <footer className="bg-light text-center py-3 mt-auto">
      <small className="text-muted">
        © {new Date().getFullYear()} Digital Book Store
      </small>
    </footer>
  );
};

export default Footer;
